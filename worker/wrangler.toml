name = "ownly-websocket"
main = "websocket-handler.js"
compatibility_date = "2024-07-01"

# WebSocket worker configuration
[env.production]
name = "ownly-websocket-prod"

[env.staging]
name = "ownly-websocket-staging"

# Optional: Add custom domain
# routes = [
#   { pattern = "ws.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]